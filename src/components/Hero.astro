<section class="relative bg-paper overflow-hidden">
  <div class="max-w-[1680px] mx-auto grid lg:grid-cols-[minmax(0,42%)_1fr] items-stretch min-h-[560px] lg:min-h-[78vh] xl:min-h-[82vh]">

    <!-- TEXT COLUMN -->
     <div class="relative px-6 lg:pl-12 lg:pr-16 xl:pr-20 py-16 lg:py-0 flex flex-col justify-center overflow-hidden min-w-0">
      <span class="pointer-events-none select-none absolute -top-4 left-4 lg:left-8 font-display font-bold text-[7rem] lg:text-[10rem] leading-none text-navy-950/[0.045] -z-10">
        1991
      </span>

      <div class="hero-fade relative min-w-0" style="animation-delay:0ms">
        <div class="flex items-center gap-3 mb-8">
          <span class="w-8 h-px bg-rust-500 shrink-0"></span>
          <p class="eyebrow text-rust-500">Bhavnagar · Alang · Gujarat, India</p>
        </div>

        <h1 class="font-display font-medium text-navy-950 text-[2.5rem] sm:text-5xl lg:text-[3.4rem] xl:text-[3.9rem] leading-[1.08] tracking-tight break-words">
          <span class="block">Go green,</span>
          <span class="block">recycle today</span>
          <span class="block italic text-rust-500">for a superior</span>
          <span class="block text-navy-950">tomorrow.</span>
        </h1>

        <p class="mt-8 text-stone-600 text-base lg:text-lg max-w-md leading-relaxed">
          Rudra Green Ship Recycling Group is a subsidiary of Rudra Global, operating an
          HKC-compliant green shipyard with full international certification across
          environment, health, and safety standards.
        </p>

        <div class="mt-10 flex flex-wrap gap-4">
          <a href="/green-ship-recycling" class="bg-rust-500 text-white px-7 py-3.5 font-medium hover:bg-rust-600 transition-colors">
            Our Recycling Process
          </a>
          <a href="/investor-relations" class="border border-navy-900 text-navy-900 px-7 py-3.5 font-medium hover:bg-navy-900 hover:text-white transition-colors">
            Investor Relations
          </a>
        </div>
      </div>
    </div>

    <!-- IMAGE COLUMN -->
    <div class="relative min-h-[420px] hero-fade" style="animation-delay: 180ms">
      <div class="absolute inset-0 lg:[clip-path:polygon(4%_0,100%_0,100%_100%,0_100%)]">
        <img src="/img/hero-shipyard.jpg" alt="Rudra Green Ship Recycling yard at Alang"
          class="w-full h-full object-cover grayscale-[10%] contrast-[1.05]" />
        <div class="absolute inset-0 bg-gradient-to-t from-navy-950/30 via-transparent to-transparent"></div>
      </div>

      <div class="hidden lg:flex absolute top-10 left-[7%] items-center gap-3 origin-top-left -rotate-90 z-10">
        <span class="w-6 h-px bg-white/60"></span>
        <span class="eyebrow text-white/80 whitespace-nowrap">Alang Ship Recycling Yard — Plot Ref. RGSR</span>
      </div>

      <span class="hidden lg:block absolute top-6 right-6 w-4 h-4 border-t-2 border-r-2 border-white/60 z-10"></span>
      <span class="hidden lg:block absolute bottom-6 right-6 w-4 h-4 border-b-2 border-r-2 border-white/60 z-10"></span>

      <!-- badge pinned to the image's own left edge — always correct, independent of headline length -->
      <div class="hidden lg:flex absolute left-0 top-1/2 -translate-x-1/2 -translate-y-1/2 z-20 hero-fade" style="animation-delay: 350ms">
        <div class="bg-white w-32 h-32 flex flex-col items-center justify-center gap-2 text-center px-4"
          style="box-shadow: 0 1px 2px rgba(14,33,66,0.06), 0 8px 16px rgba(14,33,66,0.08), 0 24px 48px rgba(14,33,66,0.10);">
          <svg width="26" height="26" viewBox="0 0 24 24" fill="none" class="text-sage-600">
            <path d="M12 2l7 3v6c0 5-3.4 8.7-7 10-3.6-1.3-7-5-7-10V5l7-3z" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round"/>
            <path d="M9 12l2 2 4-4" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          <span class="eyebrow text-navy-900 leading-tight">HKC<br/>Compliant</span>
        </div>
      </div>
    </div>
  </div>

  <div class="relative border-y border-stone-300/70 bg-white/60 overflow-hidden">
    <div class="ticker flex items-center gap-16 py-4 whitespace-nowrap">
      {Array(3).fill(['ISO 14001:2015', 'ISO 9001:2015', 'ISO 18001:2007', 'ISO 30000:2009', 'HKC COMPLIANT', 'POLLUTION CONTROL VERIFIED']).flat().map((item) => (
        <span class="eyebrow text-stone-500 flex items-center gap-16">
          {item}
          <span class="w-1 h-1 rounded-full bg-rust-500/60"></span>
        </span>
      ))}
    </div>
  </div>
</section>

<style>
  .hero-fade {
    opacity: 0;
    transform: translateY(16px);
    animation: heroFadeUp 0.7s ease-out forwards;
  }
  @keyframes heroFadeUp {
    to { opacity: 1; transform: translateY(0); }
  }

  .ticker {
    width: max-content;
    animation: tickerScroll 40s linear infinite;
  }
  @keyframes tickerScroll {
    from { transform: translateX(0); }
    to { transform: translateX(-33.333%); }
  }
</style>